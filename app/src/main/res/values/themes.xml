<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.OrbysQuizz" parent="Theme.Material3.Dark.Dialog">
        <item name="colorPrimary">@color/gray3</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/blue_selected</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:colorBackground">@color/background_black</item>
        <item name="android:windowBackground">@android:color/transparent</item>

        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/montserrat_family</item>
        <item name="android:textSize">@dimen/font_size</item>

        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>