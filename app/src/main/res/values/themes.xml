<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.OrbysQuizz" parent="Theme.Material3.DayNight">
        <!--<item name="colorPrimary">@color/gray3</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/blue_selected</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:colorBackground">@color/background_black</item>
        <item name="android:windowBackground">@color/gray3</item>

        <item name="android:textColor">@color/white</item>-->
        <item name="android:fontFamily">@font/montserrat_family</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.OrbysQuizz" parent="Base.Theme.OrbysQuizz">
    </style>

    <style name="ThemeDialog" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:fontFamily">@font/montserrat_family</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>