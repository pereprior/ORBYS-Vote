<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <!-- Permisos de internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SchoolQuizz"
        tools:targetApi="31" >
        
        <!-- Actividad principal -->
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:screenOrientation="locked"
            tools:ignore="DiscouragedApi" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad dialogo para lanzar preguntas -->
        <activity
            android:name=".ui.activities.dialogs.LaunchQuestionActivity"
            android:exported="false"
            android:theme="@style/ThemeDialog" >
        </activity>

        <!-- Actividad dialogo para añadir preguntas -->
        <activity
            android:name=".ui.activities.dialogs.AddQuestionActivity"
            android:exported="false"
            android:theme="@style/ThemeDialog" >
        </activity>

        <!-- Actividad dialogo para editar preguntas -->
        <activity
            android:name=".ui.activities.dialogs.EditQuestionActivity"
            android:exported="false"
            android:theme="@style/ThemeDialog" >
        </activity>

        <!-- Servicio http -->
        <service
            android:name=".data.server.HttpService"
            android:enabled="true" />
    </application>

</manifest>